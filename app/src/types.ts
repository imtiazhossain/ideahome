/**
 * Re-export shared types so existing app imports keep working.
 * Source of truth: @ideahome/shared
 */
export type {
  AuthProvider,
  AppTab,
  ChecklistKind,
  PendingCommentAttachment,
  TestExecutionResult,
  AssistantChatMessage,
} from "@ideahome/shared";
