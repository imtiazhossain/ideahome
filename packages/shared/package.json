{
  "name": "@ideahome/shared",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "pnpm run build && node --test tests/*.test.js"
  },
  "dependencies": {
    "@ideahome/shared-config": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
